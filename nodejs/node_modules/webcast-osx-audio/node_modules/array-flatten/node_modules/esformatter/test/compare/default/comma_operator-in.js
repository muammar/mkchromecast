// SequenceExpression (aka. comma operator)

a,b
a,b,c

a=3,foo(),   bar()   ,"baz",c

it="is"  , ("a"),(trap())

// borrowed from signals.min test a bug related to line breaks and indent
    if(e!==-1){if(a =this._bindings[e],a.isOnce()!==b)throw Error('foo');}else a=new h(this,a,b,c,d),this._addBinding(a);

